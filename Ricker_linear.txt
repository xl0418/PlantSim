
model {

# Likelihood
for ( i in 1:n) {
  mean [i] <- growth_rate * x[i] * (1 - a * x[i])
  y[i] ~ dnorm(mean[i], obs_sd)
}

# Priors
growth_rate ~ dnorm(1, 0.5)
a ~ dnorm(0.001, 0.001)
obs_sd ~ dnorm(1, 1)

# Derived quantities


} # end model

